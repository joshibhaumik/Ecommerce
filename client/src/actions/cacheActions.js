import {
  ADD_USER_TO_CACHE,
  ADD_STORE_TO_CACHE,
  ADD_ITEM_TO_CACHE
} from "./types";

export const addUserToCache = userDetails => dispatch => {};

export const addStoreToCache = storeDetails => dispatch => {};

export const addItemToCache = itemDetails => dispatch => {};
